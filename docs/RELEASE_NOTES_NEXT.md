# QuickTranslate 发布说明（vNext）

> 基线版本：v1.9.3 → 目标版本：待定
> 发布模式：咨询模式（安装包未签名，仅 SHA256 校验）
> 最近同步：2026-08-28

## 新增特性

## 优化改进

## 修复

## 仓库与流程

- 发布流程修订与工具化：版本号一键升级脚本、发布预检脚本、安装包完整性校验脚本、SHA256SUMS 附件生成；补充上传后远端 digest 复核、Latest 设置与 CDN 缓存说明、发布后收尾步骤（草稿重基线、人工验收 issue）。

## 依赖

## 验证情况

- 自动化测试：（构建后回填）
- **未执行**（需人工验收）：真实 Provider 响应、WPF 渲染、混合 DPI 放置、托盘集成、安装 / 卸载、自动更新升级、辅助功能。
- **签名**：本期为咨询模式，安装包未签名；`version.xml` 的 `<checksum>` 须在构建完整版安装包后回填真实 SHA256。
