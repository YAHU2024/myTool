<Window x:Class="QuickTranslate.UI.DownloadUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QuickTranslate 更新" Height="220" Width="420"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow" ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="StatusText" Grid.Row="0" Text="正在下载更新..." 
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>

        <ProgressBar x:Name="DownloadProgress" Grid.Row="1" Height="20" 
                     Minimum="0" Maximum="100" Value="0" IsIndeterminate="False"/>

        <TextBlock x:Name="DetailText" Grid.Row="2" 
                   Text="正在连接服务器..." FontSize="11"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   Margin="0,6,0,8" TextWrapping="Wrap" MaxHeight="60"
                   TextTrimming="CharacterEllipsis"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="CancelButton" Content="取消" Width="80" Height="28"
                    Click="CancelButton_Click" IsEnabled="True"/>
            <Button x:Name="CloseButton" Content="关闭" Width="80" Height="28"
                    Click="CloseButton_Click" Visibility="Collapsed" Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
